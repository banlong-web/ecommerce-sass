import React from 'react'
// import { useParams } from 'react-router-dom'
// import productData from '../assets/fake-data/product'

const Product = (props) => {
    // const { slug } = useParams()
    // // const slug = productData.find(slug => slug.)
  return (
    <div>
      Product
    </div>
  )
}

export default Product